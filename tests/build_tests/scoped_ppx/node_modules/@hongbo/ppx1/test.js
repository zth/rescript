#!/usr/bin/env node

// @ts-check

const fs = require('node:fs')

const file_in = process.argv[process.argv.length - 2]
const file_out = process.argv[process.argv.length - 1]

fs.createReadStream(file_in)
  .pipe(fs.createWriteStream(file_out))
